[build]
  functions = "netlify/functions"
  publish = "dist"
  command = "npx vite build"

[functions]
  node_bundler = "esbuild"
  timeout = 26

# ✅ スケジューラー設定（scheduler.ts を毎分実行）
[functions.scheduler]
  schedule = "*/1 * * * *"
